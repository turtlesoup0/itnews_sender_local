# SPEC-ITFIND-003: 인수 기준 (Acceptance Criteria)

**SPEC ID**: SPEC-ITFIND-003
**작성일**: 2026-02-04
**상태**: Planned

## 개요 (Overview)

본 문서는 SPEC-ITFIND-003의 각 요구사항에 대한 인수 기준을 Given-When-Then (Gherkin) 형식으로 정의합니다.

## REQ-1: 전자신문 1페이지 스크린샷 메일 포함

### AC-1.1: 전자신문 1페이지 이미지 추출 성공

**Given** 전자신문 PDF 파일이 존재하고
**When** `extract_first_page_for_email()` 함수를 호출하면
**Then** 1페이지의 PNG 이미지 바이트를 반환해야 하며
**And** 이미지 크기는 500KB 이내여야 하고
**And** 이미지 해상도는 DPI 200이어야 한다

### AC-1.2: 전자신문 이미지 이메일 첨부

**Given** 전자신문 PDF가 처리되었고
**When** 이메일을 생성하면
**Then** 이메일 본문 상단에 "📰 오늘의 주요 기사 미리보기" 섹션이 있어야 하고
**And** 해당 섹션에 전자신문 1페이지 이미지가 CID: `etnews_first_page`로 포함되어야 하고
**And** 이미지는 inline 방식으로 첨부되어야 한다

### AC-1.3: 전자신문 이미지 추출 실패 시 처리

**Given** 전자신문 PDF가 존재하지만
**When** 이미지 추출이 실패하면
**Then** 이메일 발송을 중단하지 않고 계속해야 하고
**And** 경고 로그를 기록해야 하고
**And** 이메일 본문에는 이미지 미포함 상태로 발송되어야 한다

### AC-1.4: 이메일 클라이언트 호환성

**Given** 전자신문 이미지가 포함된 이메일을 발송했고
**When** Gmail, Outlook, Apple Mail에서 이메일을 열면
**Then** 전자신문 1페이지 이미지가 정상적으로 표시되어야 한다

## REQ-2: 주간기술동향 PDF 메타데이터에 토픽 추가

### AC-2.1: PDF 메타데이터 저장 성공

**Given** 주간기술동향 PDF 다운로드가 완료되었고
**And** 카테고리별 토픽 추출이 성공했고
**When** PDF 메타데이터 설정을 실행하면
**Then** PDF의 Description 메타데이터에 토픽이 저장되어야 하고
**And** 저장 형식은 JSON string이어야 하며
**And** 한글 텍스트가 정상적으로 표시되어야 한다

**예시 메타데이터 형식**:
```json
{
  "기획시리즈": [
    "AI-Ready 산업 생태계 구축 방안",
    "초거대 AI 모델 활용 사례"
  ],
  "ICT 신기술": [
    "6G 핵심 기술 동향",
    "양자 컴퓨팅 상용화 전망"
  ]
}
```

### AC-2.2: PDF 뷰어에서 메타데이터 확인

**Given** 주간기술동향 PDF에 메타데이터가 저장되었고
**When** Adobe Acrobat 또는 Preview.app에서 PDF 속성을 열면
**Then** Description 필드에 카테고리별 토픽이 JSON 형식으로 표시되어야 하고
**And** 한글 텍스트가 깨지지 않고 정상 표시되어야 한다

### AC-2.3: 메타데이터 저장 실패 시 처리

**Given** 주간기술동향 PDF 다운로드는 완료했지만
**When** 메타데이터 설정이 실패하면
**Then** PDF 다운로드 작업을 중단하지 않고 계속해야 하고
**And** 경고 로그를 기록해야 하고
**And** 이메일 발송은 정상적으로 진행되어야 한다

### AC-2.4: 빈 토픽 처리

**Given** 주간기술동향 PDF 다운로드가 완료했지만
**When** 카테고리별 토픽 추출 결과가 비어있으면
**Then** 빈 JSON 객체 `{}`를 메타데이터에 저장해야 하고
**And** 에러 없이 처리되어야 한다

## REQ-3: 주간기술동향 iCloud 저장

### AC-3.1: iCloud 업로드 성공 (로컬 환경)

**Given** 로컬 환경에서 주간기술동향 이메일 발송이 완료되었고
**And** iCloud Drive가 설정되어 있고
**When** iCloud 업로드를 실행하면
**Then** `~/Library/Mobile Documents/com~apple~CloudDocs/주간 기술 동향/YYYY/` 경로에 PDF가 복사되어야 하고
**And** 파일명은 `ITFIND_주간기술동향_XXXX호_YYYYMMDD.pdf` 형식이어야 하고
**And** 로그에 업로드 완료 메시지가 기록되어야 한다

### AC-3.2: Lambda 환경에서 스킵

**Given** AWS Lambda 환경에서 실행 중이고
**When** iCloud 업로드를 실행하면
**Then** 함수를 즉시 종료하고 None을 반환해야 하고
**And** "Lambda 환경 — ITFIND iCloud 업로드 스킵" 로그를 기록해야 하고
**And** 에러 없이 처리되어야 한다

### AC-3.3: iCloud 경로 자동 생성

**Given** 로컬 환경에서 iCloud 업로드를 실행했고
**And** 대상 연도 폴더가 존재하지 않을 때
**When** 업로드를 진행하면
**Then** `주간 기술 동향/YYYY/` 폴더를 자동으로 생성해야 하고
**And** 그 안에 PDF 파일을 복사해야 한다

### AC-3.4: iCloud 업로드 실패 시 처리

**Given** 로컬 환경에서 iCloud 업로드를 실행했지만
**When** iCloud Drive가 설정되지 않았거나 권한 문제로 실패하면
**Then** 예외를 발생시키지 않고 None을 반환해야 하고
**And** 에러 로그를 기록해야 하고
**And** Lambda 함수는 정상적으로 종료되어야 한다

## 통합 인수 시나리오 (Integrated Acceptance Scenarios)

### Scenario 1: 수요일 전체 워크플로우 (로컬)

**Given** 오늘이 수요일이고
**And** 로컬 환경에서 실행 중이고
**When** 전체 워크플로우를 실행하면
**Then** 다음 순서로 작업이 진행되어야 한다:
  1. 전자신문 PDF 다운로드
  2. 전자신문 이메일 발송 (1페이지 이미지 포함)
  3. 주간기술동향 PDF 다운로드
  4. 주간기술동향 PDF 메타데이터에 토픽 저장
  5. 주간기술동향 이메일 발송
  6. 전자신문 iCloud 업로드
  7. 주간기술동향 iCloud 업로드
**And** 각 단계에서 실패가 발생해도 다음 단계는 계속 진행되어야 한다

### Scenario 2: 수요일 전체 워크플로우 (Lambda)

**Given** 오늘이 수요일이고
**And** AWS Lambda 환경에서 실행 중이고
**When** 전체 워크플로우를 실행하면
**Then** 다음 순서로 작업이 진행되어야 한다:
  1. 전자신문 PDF 다운로드
  2. 전자신문 이메일 발송 (1페이지 이미지 포함)
  3. 주간기술동향 PDF 다운로드
  4. 주간기술동향 PDF 메타데이터에 토픽 저장
  5. 주간기술동향 이메일 발송
  6. 전자신문 iCloud 업로드 (스킵됨)
  7. 주간기술동향 iCloud 업로드 (스킵됨)
**And** iCloud 업로드 단계에서 "스킵" 로그를 기록해야 한다

### Scenario 3: 수요일이 아닌 날 워크플로우

**Given** 오늘이 수요일이 아니고
**When** 전체 워크플로우를 실행하면
**Then** 다음 작업만 진행되어야 한다:
  1. 전자신문 PDF 다운로드
  2. 전자신문 이메일 발송 (1페이지 이미지 포함)
  3. 전자신문 iCloud 업로드
**And** 주간기술동향 관련 작업은 실행되지 않아야 한다

## 품질 게이트 (Quality Gates)

### 성능 기준
- 전자신문 1페이지 이미지 추출: 3초 이내
- PDF 메타데이터 설정: 1초 이내
- iCloud 업로드: 5초 이내 (로컬 환경)

### 호환성 기준
- 이메일: Gmail, Outlook, Apple Mail 호환
- PDF 메타데이터: Adobe Acrobat, Preview, Chrome PDF Viewer 호환

### 신뢰성 기준
- 이미지 추출 실패 시 이메일 발송 계속
- 메타데이터 설정 실패 시 이메일 발송 계속
- iCloud 업로드 실패 시 에러 없이 로그만 기록

## 테스트 검증 방법 (Verification Methods)

### 수동 테스트 절차

#### 1. 전자신문 이미지 테스트
```bash
# 1. 전자신문 다운로드
python -m src.scraper

# 2. 이미지 추출 테스트
python -c "
from src.pdf_image_extractor import extract_first_page_for_email
result = extract_first_page_for_email('etnews_YYYYMMDD.pdf')
print(f'Image size: {len(result)} bytes' if result else 'Failed')
"

# 3. 이메일 발송 테스트
python -c "
from src.email_sender import EmailSender
sender = EmailSender()
sender.send_bulk_email('etnews_YYYYMMDD.pdf', test_mode=True)
"

# 4. 이메일 수신 확인
# - Gmail에서 이메일 열기
# - "📰 오늘의 주요 기사 미리보기" 섹션 확인
# - 1페이지 이미지 표시 확인
```

#### 2. PDF 메타데이터 테스트
```bash
# 1. 주간기술동향 다운로드
python lambda_itfind_downloader.py

# 2. 메타데이터 확인 (macOS)
# - 다운로드된 PDF를 Preview.app으로 열기
# - 메뉴 > Tools > Show Inspector
# - Info 탭 > Description 확인
# - JSON 형식의 카테고리별 토픽 확인
```

#### 3. iCloud 업로드 테스트
```bash
# 1. 로컬에서 테스트 모드 실행
python lambda_handler.py '{"mode": "test"}'

# 2. iCloud 경로 확인
ls -la ~/Library/Mobile\ Documents/com~apple~CloudDocs/주간\ 기술\ 동향/YYYY/

# 3. 파일명 확인
# ITFIND_주간기술동향_XXXX호_YYYYMMDD.pdf
```

### 자동 테스트 (pytest)

```python
# tests/test_spec_itfind_003.py

def test_extract_first_page_for_email():
    """전자신문 1페이지 이미지 추출 테스트"""
    from src.pdf_image_extractor import extract_first_page_for_email

    # Given: 전자신문 PDF 파일
    pdf_path = "tests/fixtures/etnews_sample.pdf"

    # When: 이미지 추출
    result = extract_first_page_for_email(pdf_path)

    # Then:
    assert result is not None
    assert len(result) < 500000  # 500KB 이내
    assert result[:8] == b'\x89PNG\r\n\x1a\n'  # PNG 시그니처

def test_upload_itfind_to_icloud():
    """ITFIND iCloud 업로드 테스트"""
    from src.workflow.icloud_workflow import upload_itfind_to_icloud
    import tempfile

    # Given: 테스트 PDF 파일
    with tempfile.NamedTemporaryFile(suffix='.pdf') as tmp:
        tmp.write(b'%PDF-1.4')
        tmp.flush()

        # When: iCloud 업로드
        result = upload_itfind_to_icloud(
            tmp.name,
            issue_number="2203",
            publish_date="2026-02-04"
        )

        # Then:
        # Lambda 환경이면 None 반환
        # 로컬 환경이면 경로 반환
        assert result is None or result.endswith('.pdf')
```

## 정의 완료 (Definition of Done)

- [ ] 모든 인수 기준 (AC) 충족
- [ ] 수동 테스트 통과
- [ ] 자동 테스트 통과
- [ ] 코드 리뷰 완료
- [ ] CHANGELOG.md 업데이트
- [ ] Lambda 배포 및 테스트 완료

## 추적 가능성 (Traceability)

**TAG**: `SPEC-ITFIND-003`

**관련 SPEC**:
- SPEC-ITFIND-001: ITFIND 주간기술동향 이메일 개선 (기반 기능)
- SPEC-ITFIND-002: Chapter 기반 토픽 추출 (토픽 데이터 소스)

**인수 기준 매핑**:
- REQ-1 → AC-1.1, AC-1.2, AC-1.3, AC-1.4
- REQ-2 → AC-2.1, AC-2.2, AC-2.3, AC-2.4
- REQ-3 → AC-3.1, AC-3.2, AC-3.3, AC-3.4
